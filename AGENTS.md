# Cognitoons

## Cursor Cloud specific instructions

This repository is a newly initialized project with no source code, dependencies, or build configuration yet. The workspace contains only `README.md` and `.gitignore`.

- **No package manager or dependencies** are configured. When code is added, update the VM setup script accordingly (e.g. `npm install`, `pip install -r requirements.txt`, etc.).
- **No services to run.** There are no servers, databases, or other services defined.
- **No lint, test, or build commands** are available until a build system is introduced.
